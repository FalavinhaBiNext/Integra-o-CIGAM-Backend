## 1. Interface do Operador

- [x] 1.1 Adicionar o botão "Módulos" na tabela de listagem de empresas em `SuperAdminPanel.tsx`.
- [x] 1.2 Criar o modal `ManageModulesModal` dentro de `SuperAdminPanel.tsx` para listar todos os módulos globais e os ativos da empresa clicada.

## 2. Integração com APIs

- [x] 2.1 Ligar os checkboxes no modal às requisições do backend em `/company-modulos` (GET para carregar, POST para associar e DELETE para remover) focando no ID da empresa ativa.

## 3. Testes e Validação

- [x] 3.1 Validar a compilação do frontend (`npm run build`).
