## 1. Interface e Navegação

- [x] 1.1 Criar o componente `CompanySettings.tsx` contendo o formulário de credenciais da Cigam e checkboxes de seleção de módulos.
- [x] 1.2 Atualizar `Dashboard.tsx` para gerenciar o estado da aba ativa (`activeTab`) e renderizar condicionalmente a listagem de boletos ou o painel de configurações.

## 2. Integração com APIs

- [x] 2.1 Conectar o formulário de integração aos endpoints de `/company-integrations` (GET para carregar e POST/PUT para persistir).
- [x] 2.2 Conectar os checkboxes à listagem global de `/modulos` e controle por empresa de `/company-modulos`.

## 3. Testes e Validação

- [x] 3.1 Executar a compilação do frontend (`npm run build`) para certificar que as tipagens adicionadas estão íntegras.
