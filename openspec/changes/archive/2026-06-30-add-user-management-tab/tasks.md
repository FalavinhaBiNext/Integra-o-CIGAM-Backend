## 1. Interface e Navegação

- [x] 1.1 Criar o componente `UserManagement.tsx` contendo listagem de colaboradores e modal de formulário flutuante.
- [x] 1.2 Atualizar `Dashboard.tsx` para habilitar a aba "Usuários" (verificando privilégios `ADMIN` ou `MASTER`) e fazer a alternância condicional.

## 2. Integração com APIs

- [x] 2.1 Conectar a tabela de listagem ao endpoint `/usuarios/company/:companyId`.
- [x] 2.2 Conectar os envios de criação (POST `/usuarios`) e edição (PUT `/usuarios/:id`) bem como toggle de ativo e delete.

## 3. Testes e Validação

- [x] 3.1 Executar a compilação do frontend (`npm run build`) para certificar integridade.
