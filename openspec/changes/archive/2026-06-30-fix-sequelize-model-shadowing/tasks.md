## 1. Refatoração dos Modelos

- [x] 1.1 Atualizar `Boleto.ts` alterando as propriedades de `public` para `declare`.
- [x] 1.2 Atualizar `Company.ts` alterando as propriedades de `public` para `declare`.
- [x] 1.3 Atualizar `CompanyIntegration.ts` alterando as propriedades de `public` para `declare`.
- [x] 1.4 Atualizar `CompanyModulo.ts` alterando as propriedades de `public` para `declare`.
- [x] 1.5 Atualizar `Modulo.ts` alterando as propriedades de `public` para `declare`.
- [x] 1.6 Atualizar `UserType.ts` alterando as propriedades de `public` para `declare`.
- [x] 1.7 Atualizar `Usuario.ts` alterando as propriedades de `public` para `declare`.

## 2. Validação e Testes

- [x] 2.1 Executar a suíte de testes de integração (`npm test`) para verificar o funcionamento do carregamento de atributos.
